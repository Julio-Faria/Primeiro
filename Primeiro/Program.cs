using System; // referencia para o name space

namespace PrimeiroProjeto
{
    class Program
    {
        static void Main(string[] AssemblyLoadEventArgs) //começo de execução do programa
        {
            Console.WriteLine("Olá Mudo!");
        }
    }
}